<Window x:Class="wpf_image_to_text_tesseract_ocr_application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_image_to_text_tesseract_ocr_application"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Button x:Name="StartButton" Click="StartButton_Click" Content="Start" Margin="15" Background="Blue" Foreground="White" FontSize="18"/>
        <Button x:Name="StopButton" Click="StopButton_Click" Content="Stop and Clear" Margin="15" Grid.Column="1" Background="Blue" Foreground="White" FontSize="18"/>
        <TextBox  x:Name="textBox" Grid.Row="1" Grid.ColumnSpan="2" Background="Black" Margin="15" Foreground="White"/>
    </Grid>
</Window>
